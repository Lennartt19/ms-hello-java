# Fallback secret value (used if environment variable SECRET_VALUE is not set)
# secreto.fallback=valor-de-fallback

# Lista de variables de entorno que se mostrarán en /secreto (separadas por coma)
# secreto.vars=SECRET_VALUE,APP_NAME,APP_VERSION,USER,HOSTNAME
